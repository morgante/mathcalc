Math Calculator

How to use
Create a new Calc class, and pass the math expression to the constructor:
$calc = new Calc($expression);

Output the interpretation of the roll:
echo $calc->infix();

Output the result of the roll:
echo $calc->calc();